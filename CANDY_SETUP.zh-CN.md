# 丘丘糖果色匣 · 测试版部署

1. 把本次更新的 `frontend-dist/studio/`、`functions/api/studio/[[path]].js` 和 `database/migrations/studio-candy.sql` 按原目录上传到 GitHub 仓库。保留已有文件，不要只上传 ZIP。
2. 在 Cloudflare 控制台打开图床使用的 D1 数据库 `qiuqiu_d1` →「控制台」。打开 `database/migrations/studio-candy.sql`，复制里面的两段 `CREATE TABLE` 语句，粘贴执行一次。看到“查询已成功执行”即可，不会修改旧图和已有账号。
3. 等 Pages 的新部署成功，刷新 `/studio/`，登录后到「丘丘糖果色匣 · 测试版」。上方应显示今日剩余份数。

每次**生成新美化**计入当天额度：皮丘 2 份、皮卡丘 10 份、超级无敌美化大师丘不限量；调色预览和保存草稿不扣次数。每日北京时间 00:00 重新计数。网络中断时，同一草稿项目可重试导出，不会再次扣次数。

图片预览在浏览器里完成。只上传实际改过的图片，保存到以新美化名称命名的相册；没有修改的图链保留原样。旧图和旧美化文件不会被删除。草稿保存在**当前浏览器的 IndexedDB**，其他设备看不到；清理浏览器网站数据会删除草稿。手动上传的替换图片也保存在草稿中，直到最终导出才占用相册存储额度。

外站图片的改色/下载走登录后受限的来源代理。当前支持 `iili.io`、`i.postimg.cc`、`img.baidu.re`、`img.baibai.cv`、`771553.xyz`、`qianqianqiu.date`，还可通过现有 `STUDIO_IMPORT_HOSTS` 添加可信图片域名。其他来源请先下载图片，再在对应条目中上传手改图。每张最多 25 MB，画布最多 2000 万像素。GIF 动图经画布改色后只保留第一帧并导出透明 PNG；不改的 GIF 保持原图链。浏览器可能会拦截「下载全部原图」触发的多个下载，请在提示出现时允许本站下载多个文件。

手机 BG0 已加防崩溃保护，手机继续使用「轻巧快抠」，桌面仍可使用 BG0。
